package entidad;
/**
 * Interface que define el comportamiento de una llave en el juego.
 * Permite obtener y establecer el estado de la llave. Lo implementan
 * las clases que representan llaves en el juego.
 */
public interface Llave {
    public boolean getLlave();
    public void setLlave();
}
